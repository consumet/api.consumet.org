import { IBookProviderParams, LibgenBook } from './types';
import animeSchema from './schema/anime.schema';
import { libgenModel, getComicsModel } from './models';

export { IBookProviderParams, animeSchema, libgenModel, getComicsModel, LibgenBook };
